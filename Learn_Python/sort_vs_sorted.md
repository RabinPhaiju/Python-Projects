1. sort vs sorted
   - sort1 = ['a', 'z', 'g', 'k', 'h']
   - sort2 = [1, 4, 6, 54, 3]
   - sort1.sort() # modify the orignal list
   - print(sort1)
   - print(sorted(sort1)) # wont change the original list
